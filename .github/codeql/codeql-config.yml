# CodeQL configuration for the imgsearch project
# This config adds custom sanitizer models for path validation

name: "ImgSearch CodeQL Config"

# Use default queries plus our custom extensions
queries:
  - uses: security-extended

# Include our custom data extensions for path sanitization
data-extensions:
  - .github/codeql/path-sanitizers.yml
